<div class="d-flex justify-content-between">
    <h1>{{title}}</h1>
    <a href="/new/transaction" target="_blank" class="btn btn-secondary btn-sm align-middle" style="height: 2.2em;"><small>New Transaction</small></a>
</div>

<div class="accordion accordion-flush" id="accordionFlushExample">
    {{#if transactions}}
        {{#each transactions}}
        <div class="accordion-item">
            <h2 class="accordion-header" id="flush-heading{{id}}">
                <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#flush-collapse{{id}}" aria-expanded="false" aria-controls="flush-collapse{{id}}">
                    {{item}}: {{amount_f}} | {{date_f}}
                </button>
            </h2>
            <div id="flush-collapse{{id}}" class="accordion-collapse collapse" aria-labelledby="flush-heading{{id}}" data-bs-parent="#accordionFlushExample">
                <div class="accordion-body">
                     <ul class="list-group">
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            {{category}}
                            {{merchant}}
                            {{project}}
                            <a href="/delete/transaction/{{@key}}" class="btn btn-danger">Delete</a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
        {{/each}}
    {{else}}
    <p>No transactions yet...</p>
    {{/if}}
</div>

<!--
<form action="/new/transaction" method="POST">
    <input name="item" type="text" style="display: none" value="Hamburger">
    <input name="merchant" type="text" style="display: none" value="McDonald's">
    <input name="amount" type="number" style="display: none" value="-150">
    <input name="date" type="datetime-local" style="display: none" value="2022-09-20T18:10">
    <input name="project" type="text" style="display: none" value="Frühlingsball">
    <input name="category" type="text" style="display: none" value="Food">
    <input name="type" type="text" style="display: none" value="Expense">
    <input name="account" type="text" style="display: none" value="Cash">
    <input name="uid" type="text" style="display: none" value="0000001">
    <input type="submit" class="btn btn-primary" value="Add Demo Data">
</form>
-->